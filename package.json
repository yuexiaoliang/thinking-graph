{
  "name": "thinking-graph",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "check": "node scripts/validate-graph.mjs",
    "dev": "astro dev",
    "build": "npm run check && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.3",
    "js-yaml": "^4.1.0",
    "marked": "^18.0.13",
    "sanitize-html": "^2.17.6"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
